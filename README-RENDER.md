# Egg Sales System — Render Blueprint
1. Upload this folder to a NEW GitHub repository.
2. Render Dashboard → New → Blueprint.
3. Connect the repository containing `render.yaml`.
4. Review the Web Service and Postgres database.
5. Deploy Blueprint.

Login: admin / admin123
Payment methods: Cash, Mobile Money, Bank only.

This version uses PHP/Apache in Docker and Render Postgres. Render's free-plan availability can change; the Render Dashboard will show the current billing requirements.
